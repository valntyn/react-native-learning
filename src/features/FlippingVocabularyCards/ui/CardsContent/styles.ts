import { StyleSheet } from 'react-native';

export const cardStyles = StyleSheet.create({
    card: {
        alignItems: 'center',
        borderRadius: 10,
        borderWidth: 1,
        display: 'flex',
        height: 200,
        justifyContent: 'center',
        width: '100%',
    },
});
